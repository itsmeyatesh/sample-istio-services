deployment:
  docker_hub:
    branch: master
    build_dir: caller-service
    commands: 
      - $DOCKER_TRIGGER_CALLER_SERVICE
    build_dir: callme-service
    commands: 
      - $DOCKER_TRIGGER_CALLME_SERVICE